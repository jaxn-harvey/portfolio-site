// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom domain via CNAME (public/CNAME), so site root is "/".
  site: 'https://jiharvey.com',
});
